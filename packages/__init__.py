# -*- coding: utf-8 -*-

import os
from os.path import join as joinPath

ROOT_DIR = os.path.abspath("../")
inputDir = joinPath(ROOT_DIR, "input")
outputDir = joinPath(ROOT_DIR, "output")
filesDir = joinPath(ROOT_DIR, "files")